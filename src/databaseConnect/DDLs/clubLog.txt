CREATE TABLE ClubLog
(
Timestamp VARCHAR(255) PRIMARY KEY NOT NULL,
Date VARCHAR(255),
StudentID INTEGER,
ClubID INTEGER,
FOREIGN KEY (StudentID) REFERENCES Student(StudentID),
FOREIGN KEY (ClubID) REFERENCES Cub(ClubID)
);
