CREATE TABLE SubjectGraph
(
StartVertex INTEGER NOT NULL,
EndVertex INTEGER NOT NULL,
Weight INTEGER,
FOREIGN KEY (StartVertex) REFERENCES Subjects(SubjectID),
FOREIGN KEY (EndVertex) REFERENCES Subjects(SubjectID),
PRIMARY KEY (StartVertex, EndVertex)
);